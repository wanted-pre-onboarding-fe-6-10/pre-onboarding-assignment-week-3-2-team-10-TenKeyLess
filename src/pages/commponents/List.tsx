import { useEffect } from 'react';
import JasonServerApi from '../../api/api';

const List = () => {
  /* api 호출 */
  // const getItems = new JasonServerApi();
  // const getList = async () => {
  //   const res = await getItems.getComments();
  // };

  // useEffect(() => {
  //   getList();
  // }, []);

  return <div>list</div>;
};

export default List;
